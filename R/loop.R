loop <- function (graph, depth, messages, permutation) {

        for (vert in permutation) {
                if (V(graph)$root[vert] == TRUE) {

                }

        }

        return (TRUE);
}
